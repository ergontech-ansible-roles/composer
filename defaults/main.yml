---

composer_version: 1.6.4
composer_use_latest: true
composer_user: "{{ ansible_ssh_user }}"
composer_deps:
  - curl
  - git
  - unzip

composer_auth_file: "/home/{{ composer_user }}/.config/composer/auth.json"